# rustyTCP
an implementation of TCP with Rust

I want to create a TUN device in linux user space so I don't mess up my kernel's preexisting TCP implementation.

1. check if we can send / receive packets